<UserControl x:Class="Presentacion.Views.DayAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:Presentacion.Converters"
             mc:Ignorable="d"
             Height="450" Width="400">
    <UserControl.Resources>
        <converters:CitaStatusConverter x:Key="CitaStatusConverter"/>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="10" Padding="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <TextBlock x:Name="TxtHeaderDate" Text="Lunes, 12 Diciembre" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button x:Name="BtnBack" Content="← Volver" Visibility="Collapsed" Click="BtnBack_Click" HorizontalAlignment="Right"
                        Background="Transparent" Foreground="#3498DB" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
            </Grid>

            <!-- List View -->
            <Grid x:Name="ListViewGrid" Grid.Row="1">
                <TextBlock x:Name="TxtNoCitas" Text="No hay citas para este día." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#95A5A6" Visibility="Collapsed"/>
                
                <ListBox x:Name="LstCitas" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,5"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="#ECF0F1" BorderThickness="0,0,0,1" Padding="10" Cursor="Hand">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F7F9F9"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <!-- Click Event Trigger via CodeBehind (SelectionChanged) -->
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/> <!-- Time -->
                                    <ColumnDefinition Width="*"/>    <!-- Info -->
                                    <ColumnDefinition Width="Auto"/> <!-- Status -->
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding HoraStr}" FontWeight="Bold" Foreground="#2C3E50" FontSize="14" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding NombrePaciente}" FontWeight="SemiBold" Foreground="#34495E"/>
                                    <TextBlock Text="{Binding Tratamiento}" FontSize="12" Foreground="#7F8C8D"/>
                                </StackPanel>

                                <Border Grid.Column="2" Background="{Binding Estatus, Converter={StaticResource CitaStatusConverter}}" CornerRadius="8" Padding="8,2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Estatus}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- Detail View -->
            <Grid x:Name="DetailViewGrid" Grid.Row="1" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Información de la Cita" FontSize="16" FontWeight="SemiBold" Foreground="#3498DB" Margin="0,0,0,15"/>
                    
                    <Border Background="#F4F6F7" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Hora:" FontWeight="Bold" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="DetailHora" Text="10:00" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#2C3E50"/>
                            </Grid>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Estatus:" FontWeight="Bold" Foreground="#7F8C8D"/>
                                <TextBlock x:Name="DetailEstatus" Text="Confirmada" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#27AE60"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Paciente" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5"/>
                    <TextBlock x:Name="DetailPaciente" Text="Juan Perez" FontSize="14" Foreground="#34495E" Margin="0,0,0,15"/>

                    <TextBlock Text="Tratamiento" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5"/>
                    <TextBlock x:Name="DetailTratamiento" Text="Consulta General" FontSize="14" Foreground="#34495E" Margin="0,0,0,15"/>

                    <TextBlock Text="Dentista" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5"/>
                    <TextBlock x:Name="DetailDentista" Text="Dr. House" FontSize="14" Foreground="#34495E" Margin="0,0,0,15"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
