<UserControl x:Class="Presentacion.Views.DentistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentacion.Views"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SideMenuColumn" Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Dentista (Color Azul Médico) -->
        <Border Grid.Column="0" Background="#3498DB">
            <StackPanel Margin="10">
                <!-- Boton hamburguesa -->
                <Button x:Name="BtnHamburger" Width="30" Height="30" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="Bold" FontSize="16" Cursor="Hand"
                        HorizontalAlignment="Right" Margin="0,10,0,0" Click="BtnHamburger_Click">
                    <Image Source="pack://application:,,,/assets/icons/dashboard.png" Stretch="Uniform"/>
                </Button>
                <Image Source="pack://application:,,,/assets/icons/person.png" Width="60" Height="66" HorizontalAlignment="Center" Margin="0,20,0,10"/>

                <TextBlock Text="Dr. Perfil" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,5,0,10" HorizontalAlignment="Center"/>
                <TextBlock Text="Odontología General" Foreground="#D6EAF8" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <Button Content="Mi Agenda Hoy" Height="40" Margin="0,5" Background="#2980B9" Foreground="White" BorderThickness="0"/>
                <Button Content="Mis Pacientes" Height="40" Margin="0,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                <Button Content="Tratamientos" Height="40" Margin="0,5" Background="Transparent" Foreground="White" BorderThickness="0"/>
                <Button Content="Cerrar Sesion" Height="40" Margin="0,5" Click="BtnCerrarSesionClick" Background="Transparent" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <!-- Contenido Dentista -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Pacientes Programados" FontSize="22" Foreground="#2C3E50"/>
                <TextBlock Text="(19 Nov 2025)" FontSize="16" Foreground="Gray" VerticalAlignment="Bottom" Margin="10,0,0,2"/>
            </StackPanel>

            <!-- Lista de Citas -->
            <DataGrid Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
                      Background="White" BorderThickness="1" BorderBrush="#EEE">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Hora" Width="80" Binding="{Binding Hora}"/>
                    <DataGridTextColumn Header="Paciente" Width="*" Binding="{Binding NombrePaciente}"/>
                    <DataGridTextColumn Header="Motivo" Width="150" Binding="{Binding Motivo}"/>
                    <DataGridTextColumn Header="Estado" Width="100" Binding="{Binding Estado}"/>
                    <DataGridTemplateColumn Header="Acción" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Atender" Background="#27AE60" Foreground="White" Padding="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
                <!-- Datos Dummy para vista de diseño -->
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
