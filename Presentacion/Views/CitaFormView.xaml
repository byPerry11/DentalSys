<UserControl x:Class="Presentacion.Views.CitaFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="500" Width="400">
    <Border Background="White" CornerRadius="10" Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Nueva Cita" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Paciente" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox x:Name="CmbPaciente" Height="30" DisplayMemberPath="Nombre" SelectedValuePath="Id"/>

                <TextBlock Text="Dentista" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox x:Name="CmbDentista" Height="30" DisplayMemberPath="Username" SelectedValuePath="Id"/>

                <TextBlock Text="Fecha" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <DatePicker x:Name="DpFecha" Height="30"/>

                <TextBlock Text="Hora" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox x:Name="CmbHora" Height="30"/>

                <TextBlock Text="Estatus" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox x:Name="CmbEstatus" Height="30">
                    <ComboBoxItem Content="Pendiente" IsSelected="True"/>
                    <ComboBoxItem Content="Confirmada"/>
                    <ComboBoxItem Content="Cancelada"/>
                    <ComboBoxItem Content="Completada"/>
                </ComboBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cancelar" Width="100" Height="35" Margin="0,0,10,0" Click="BtnCancelar_Click"
                            Background="White" Foreground="#7F8C8D" BorderBrush="#BDC3C7" BorderThickness="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="Guardar" Width="100" Height="35" Click="BtnGuardar_Click"
                            Background="#27AE60" Foreground="White" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
